# <项目名>

<!-- 维护须知（Claude Code 注入上下文前会剥离 HTML 注释；Codex 等其他工具会原样读到，故保持精简）：
     本文件是项目说明的唯一事实来源，CLAUDE.md 是指向它的软链——只改这里。
     尽量 ≤60 行、硬上限 200 行，只写 agent 猜不到的信息。
     每行自问「删掉它 agent 会犯错吗？」不会就删。
     owner：<负责人>；改动走 PR review；每 3-6 个月修剪一次。 -->

<一句话说明这个项目是做什么的>

## 常用命令

- 构建：`<TODO>`
- 测试：`<TODO>`
- Lint：`<TODO>`

## 技术栈与约定

- <技术栈及版本>
- <与默认不同的代码约定；没有就删掉本节>

## 仓库礼仪

- <分支命名 / commit 约定 / PR 约定>

## 已知的坑

- <必需的环境变量、非显而易见的行为；没有就删掉本节>
