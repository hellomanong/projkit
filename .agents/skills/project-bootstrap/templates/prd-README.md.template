# 外来输入登记

<!-- 本目录是外来输入区：上游给的文档（PRD、原型、外部架构设计等）放这里，只读、不是事实来源。
     本文件登记全部外部参考的去处——目录或 URI，每条一行：位置 + 一句是什么。
     skill 与日常会话都按本表找上游文档；位置变了随手更新本表。
     agent 打不开的线上文档（Figma、wiki 等）：重要内容落一份快照进本目录，URI 只当溯源线索。
     条目没有就写「无」，不够就加行；路径一律写仓库根相对路径或完整 URI。
     ※ 本表分两段，读法不同——A 段可以写进 SPEC，B 段只用来核对。删掉分段等于让 skill
       把「现在代码是这样」当成决定抄进 SPEC，代码一改 SPEC 就变成谎话。 -->

## A. 依据类——稳定依据，可以写进 SPEC

- PRD 目录：<路径或 URI；多份就逐份列，标明哪份是正本>
- 需求目录：<需求文档 / 用户故事 / 验收标准的位置>
- 架构目录：<外部架构设计文档的位置——`/spec-interview 全局` 与 `/spec-design` 的登记模式会先查这里>
- 决策目录：<ADR / 决策记录的位置>
- 契约目录：<接口与事件定义的正本位置；**照抄接口签名时注明出处**>
- 领域术语：<术语表 / 领域模型的位置>
- 工程约定：<技术栈、服务配置等长期工程约定的位置>
- 设计目录：<UI / 交互设计稿的位置（Figma 链接、切图目录等）>
- 其他参考：<竞品分析、合规要求等>

## B. 现状类——当前状态，只查不抄

<!-- B 段的唯一用法是核对。不作为决定来源，不进 SPEC 正文。
     确需写进 SPEC 时必须标日期 + 注明「现状，非决定」。 -->

- 代码目录：<各服务/模块的代码位置——查已经建成什么，避免 SPEC 重新规定一遍已有的东西>
- 工作项：<GitHub Issues / Project 的 owner 与编号——查本期里程碑范围、谁负责、卡在哪>
- 测试目录：<测试与测试工具的位置——查已有验证覆盖到哪>
- 部署与运行：<部署配置、运行手册的位置>
