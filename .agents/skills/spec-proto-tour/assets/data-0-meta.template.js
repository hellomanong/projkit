// 讲解数据的公共部分。复制到讲解数据目录并改名为 data-0-meta.js，按本项目填写；<…> 是要替换的占位。
window.AGS_ANNO_VIEWS = [];

// 模块词表：卡片「归属模块」只能用这里的键。粒度细到能回答「这件事到底是谁干的」，通常要到服务里的子模块。
// n 是标签上的名字；g 决定标签颜色（console、gc、gcs、litellm、si、envoy、usage、deploy、ext 九种，按角色借用即可）；
// d 是鼠标悬停和图例里的一句白话说明，不写契约编号、文件名。
window.AGS_ANNO_MODULES = {
  'console':  { n: 'Console',         g: 'console', d: '<管理控制台页面：只调用后端接口，自己不存数据。>' },
  '<svc-sub>': { n: '<服务 · 子模块>', g: 'gc',      d: '<它负责保存什么、决定什么，一句白话。>' },
  '<svc2>':   { n: '<服务2>',          g: 'gcs',     d: '<它负责把什么送到哪里；是否经手用户的每次请求。>' },
  'ext':      { n: '外部数据源',       g: 'ext',     d: '<项目之外的系统。>' }
};

// 现状五档，固定不改
window.AGS_ANNO_STATUS = {
  done: { m: '●', t: '已实现' },
  part: { m: '◐', t: '部分实现' },
  no:   { m: '○', t: '未实现' },
  na:   { m: '—', t: '设计未覆盖' },
  out:  { m: '·', t: '不在范围' }
};

window.AGS_ANNO_META = {
  kicker: '原型讲解标注 · <主题>',
  scope: '<主题>相关的页面',
  indexIntro:
    '**先讲全局，再讲每一页。**\n\n' +
    '**1. 原型版本**：<原型对应哪一版 PRD；PRD 现行版本；原型是否已同步>。讲解时把三件事分开说：原型画的是什么，PRD 把它排在哪个阶段，后端现在做到哪一步。\n\n' +
    '**用词**：<本主题最容易混的对象名，统一叫什么，第一次出现给一句定义>。\n\n' +
    '**2. 一条配置怎么生效**：<从页面操作到后端生效，按模块词表里的名字讲一遍链路>。\n\n' +
    '**3. 一次调用怎么走**：<运行时一次请求经过哪些模块，谁在路上检查、谁执行、谁记账>。\n\n' +
    '**4. 一句话分工**：<每个模块一句>。\n\n' +
    '**5. 卡片怎么读**：「归属模块」列出这个功能由哪些后端模块负责、各自做什么，标「（目标）」的是设计目标、还没做；鼠标停在模块标签上能看到模块说明，下方有完整图例。「现状」用五档标记：● 已实现、◐ 部分实现、○ 未实现、— 设计未覆盖、· 不在本次范围。\n\n' +
    '**6. 标注用法**：圆点编号对应面板里的卡片，点圆点跳到卡片，点卡片把页面滚到对应元素。拖动面板标题栏可以移动面板，拖右下角可以缩放；「折叠」只留标题栏，「隐藏」收起面板但保留圆点；底部居中的黑色按钮一键开关全部标注，这个按钮本身也能拖动。「带我去」会自动点击原型导航到对应页面或抽屉，「只看讲解」只切换面板内容。',
  sources: '生成日期 <YYYY-MM-DD>。依据：<原型路径与版本>、<PRD 路径与版本>、<设计文档>、<代码仓库与 commit>。源文档更新后需要同步修订这份标注。',
  foot: '标注生成于 <YYYY-MM-DD>，依据 <PRD 版本>、<设计文档> 与 <commit> 的源码。「现状」只写仓库里能核对的事实；未经运行验证的结论会写明。'
};
